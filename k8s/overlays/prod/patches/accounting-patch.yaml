apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounting
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: accounting
          resources:
            requests:
              cpu: 400m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi