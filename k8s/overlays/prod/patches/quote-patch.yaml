apiVersion: apps/v1
kind: Deployment
metadata:
  name: quote
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: quote
          resources:
            requests:
              cpu: 400m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi