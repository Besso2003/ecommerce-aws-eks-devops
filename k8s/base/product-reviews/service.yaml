apiVersion: v1
kind: Service
metadata:
  name: product-reviews
  labels:
    app: product-reviews
spec:
  type: ClusterIP
  selector:
    app: product-reviews
  ports:
    - port: 3551
      targetPort: 3551