apiVersion: v1
kind: Service
metadata:
  name: flagd
  labels:
    app: flagd
spec:
  type: ClusterIP
  selector:
    app: flagd
  ports:
    - name: grpc
      port: 8013
      targetPort: 8013
    - name: ofrep
      port: 8016
      targetPort: 8016